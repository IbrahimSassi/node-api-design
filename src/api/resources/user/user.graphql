type User {
  id: ID!
  friends: [String]
  username: String!
  createdAt: String!
  updatedAt: String!
}

input UpdatedUser {
  username: String!
}

type Query {
  getMe: User!
  getAll: [User]!
}

type Mutation {
  updateMe(input: UpdatedUser): User!
}